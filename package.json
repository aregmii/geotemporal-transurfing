{
  "name": "atlas-of-when",
  "version": "0.1.0",
  "private": true,
  "description": "A globe with a time slider: spin to a place, slide to a moment, see what happened there and what was happening elsewhere.",
  "scripts": {
    "build": "node pipeline/build.js",
    "refresh": "bash pipeline/refresh.sh",
    "refresh:all": "bash pipeline/refresh.sh --all",
    "dev": "python3 -m http.server 8000 --directory site",
    "playground": "node pipeline/playground.js",
    "build:sky": "node pipeline/build_sky.js && node pipeline/mw_mask.js && python3 pipeline/bake_sky.py",
    "media": "cd pipeline && python3 fetch_media.py ../site/data/events.json --from 1926 --min-weight 3 --media-dir ../site/media --out ../site/data/media.json"
  },
  "license": "MIT",
  "devDependencies": {
    "d3-celestial": "0.7.35"
  }
}
